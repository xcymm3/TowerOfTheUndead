# 直接开发第 4 步：终局闭环

冻结日期：2026-09-18。按用户指令直接开发，不运行 Deadline-Carl，不使用子代理。

- AC1：从满足 Pelle 前置的未封界状态，经界面确认进入终焉；随后不直接写入 `progressBits`、裂隙完成位、织机阶段、牺牲标志或结局标志，通过原事件/条件依次触发五次封印，记录每次规则变化、奖励和解锁。
- AC2：终焉禁用规则、主要重置/跨终焉保留项、全部非重复终焉升级及重复升级价格边界通过原控件或原购买方法验证；装备槽失效与恢复使用原封印/里程碑路径，结果与未换肤 reference 一致。
- AC3：五条裂隙的资源、同时吸取上限、15 个里程碑、满额停止和效果变化通过原控件及原计时逻辑验证，不修改裂隙完成状态。
- AC4：万墓织机从原解锁条件进入，五项升级、五次有序牺牲、裂隙失效与 200% 恢复均由原按钮和原循环推进；最后以真实游魂生产/结局检查触发 `GameEnd`, 不直接设置 `player.isGameEnd` 或 `GameEnd.endState`。
- AC5：实际完成结局淡出、字幕显示/关闭及新游戏入口的可见与交互检查；验证结局前后关键状态、字幕文本和退出路径与原版一致。
- AC6：同步修复 Pelle、裂隙、织机、结局与字幕的关键文案、低对比、裁切和窄屏不可操作问题；在 320/375/414/768/1024/1440 留存关键页面截图及控件边界记录。
- AC7：当前源码通过终焉定向回归、820 文件/767 映射/模拟基线校验、类型、lint、完整构建和正式产物冒烟；证据明确区分阶段资源夹具、原事件操作、真实结局触发，以及第 5–7 步仍未完成范围。

约束：上游 `5409e320cecef96a917cca1dfb68f1f183e499ca`、原玩法数值、费用、顺序、随机、重置/保留规则和发布验收标准不变；不修改 `vendor/antimatter` 模拟内核。允许为缩短长线等待注入达成下一事件所需的资源和历史值，但不得直接设置本 AC 所验收的封印、裂隙、织机或结局结果字段。767 条逐项显示/语义审计归第 5 步，历史/损坏存档与集中全量回归归第 6 步，Pages 发布归第 7 步。

交付：生成 `evidence.md`、`evidence.json`、原始报告、`verdict.json` 与 `problems.md`；当前代码新一轮验证全部通过后，只提交并推送本步相关文件。
