# 亡灵之塔：开发进度与五步计划

盘点日期：2026-09-17。本次仅整理进度和后续任务，不把中断前的未提交实现视为正式交付。

## 固定要求

- 玩法、公式、价格、倍率、解锁、重置保留规则、随机机制和离线模拟均以 `AntimatterDimensionsSourceCode` 提交 `5409e320cecef96a917cca1dfb68f1f183e499ca` 为准，不自行设计或调整平衡。
- 名称与对象对应遵循《亡灵之塔-逐项内容映射表.md》和《反物质维度-映射参数索引.json》，保留源 ID 和对应关系。内部标识保留原名时，必须仍能精确追溯映射。
- 采用已确认的暗黑像素风。最新要求为左侧军团展示，经营位于右侧；旧参考说明和当前代码仍是左经营、右军团，待第 2 步纠正。
- 军团场景只展示真实拥有的单位，不引入战斗、伤害、掉落、死亡消耗或额外收入。
- 每一步独立验证、记录结果并提交推送，避免一次性完成全部工作。已有无关改动不混入提交。

## 当前实际进度

| 部分 | 已存在的实现 | 尚不能认定完成的部分 |
|---|---|---|
| 源码与数值基础 | `vendor/antimatter` 保留原作；构建脚本在临时目录应用展示和平台适配；哈希清单覆盖 820 个文件 | 从当前源码重新完整构建并复核适配边界、依赖及可复现性 |
| 映射 | 767 条记录绑定源配置，具备词汇表、短语适配和典籍查询 | 绑定成功不等于所有动态说明、奖励、弹窗和后期页面已严格完成中文映射 |
| 界面 | React 外壳、资源栏、墓园背景、八族精灵、军团名册；Vue 内核通过 iframe 接入 | 左右位置与最新要求不同；后期主要沿用原作组件，视觉和长文本仍需逐页验收 |
| 初期玩法 | 自定义军团页调用原购买、节律、筑塔、墓域及献祭逻辑 | 补齐边界交互与解锁提示验收 |
| 中后期 | 复用原作转生、轮回、创世、冥器、主宰及终局系统和页面 | 不等于已逐系统完成可操作性、文案、重置边界与全流程验收 |
| 存档与离线 | 独立本地存档键，沿用原序列化与离线模拟，已有保存和重载测试 | 导入导出、备份恢复、多槽、异常输入和升级兼容仍需专项验证 |
| 测试 | 已有源码校验、原版与主题版场景对照、浏览器测试和截图 | 测试仅抽样覆盖；强制解锁页面渲染不证明正常解锁流程完整 |
| 文档与版本 | 设计、映射及参考图已在历史提交中 | 中断前的大量实现仍未提交；旧 README 的“只有静态页面”描述已过时 |

实际架构为 React 外壳 + 原作 Vue 运行时，并非原设计提案中的全量 React 展示重写。后续以保留原数学规则和完成可用界面为目标，避免无必要的内核重写。

## 本次验证记录及限制

- `node scripts/verify-source.mjs`：通过，820 个上游文件未改变，767 个映射键唯一，现有构建目录中受检查的模拟源码与基线一致。
- `node node_modules/typescript/bin/tsc -b --pretty false`：通过。
- `pnpm lint`：退出码 0。
- `node tests/runtime.mjs`：报告 31 项通过、0 项失败，浏览器错误列表为空；包含 22 个数值对照场景、41 个页面渲染以及 320/375/414/768/1440 像素宽度检查。
- 浏览器测试使用现有 `public/engine` 与 `public/reference` 构建产物。本次没有重新运行完整构建；部分运行时源文件晚于现有产物，因此这些结果不能替代第 1 步的重新构建验收。
- 尚未完成自然推进至结局的全流程验收，不能据此宣称完整游戏开发完成。不估算缺乏依据的完成百分比。

## 剩余工作：严格分五步

### 第 1 步：恢复可复现基线，收口中断现场

检查并归属所有未提交文件；核对原作提交、源码清单、适配白名单、依赖锁文件、构建脚本与存档隔离。重新构建原版对照与亡灵版，解决构建或数值对照失败，整理当前架构说明和实际启动命令。清理旧占位状态前先确认无引用。

验收：从源码完整构建成功，源码校验、类型检查、lint 和现有 31 项回归通过；确认测试针对本轮新产物。提交仅包含已核对的基线实现，并记录提交号。

### 第 2 步：完成确认风格与初期可玩闭环

调整为左军团、右经营；按参考图统一面板、按钮、字体、像素精灵与数值排版。展示真实单位和数量，动画暂停仅影响画面。核对八阶购买、每十购、最大购买、魂火节律、筑塔、墓域、献祭，以及首次转生的入口、条件与确认提示。

验收：真实点击完成购买和满足原条件的初期重置；军团显示与存档同步；桌面和移动端无关键按钮遮挡、溢出；视觉截图逐项核对参考风格。

### 第 3 步：完成转生至轮回、永夜阶段

逐页完成魂契、打破魂界、通灵议会、疫种、亡灵/破界/轮回试炼、执役自动购买、墓园祭坛、冥典树、轮回里程碑及永夜的映射和界面。验证研究前置、互斥、重洗、挑战特殊规则与重置保留项。

验收：每个系统均有与原版同状态的关键操作对照；覆盖购买成功与失败、解锁边界及重置；不能只靠开发测试强制打开页面。记录该阶段映射和页面检查清单。

### 第 4 步：完成创世、七位主宰与终局

逐页完成创世律令、亡者遗赠、冥器生成/装备/祭业/筛选、死寂双钟、塔灵敕令、七位主宰领域、追忆、炼金、虚冥、灾厄化身、死星、终焉封界、裂隙和万墓织机。保留原随机种子行为及敕令语法，不以词汇替换破坏脚本。

验收：各领域进入/退出和重置边界与原版一致；冥器随机与装备效果可对照；覆盖终焉规则变化、万墓织机阶段和结局触发。关键操作必须有可达入口，页面可渲染不能作为唯一验收依据。

### 第 5 步：全流程审计与交付

逐项审计 767 条映射及数据库之外的资源、阶位、动态说明、帮助、通知、确认框和结局文字。补齐导入导出、备份、多存档槽、离线、异常恢复测试；检查后期大数排版与性能。运行全量回归及跨阶段操作链，构建正式产物，补全文档、许可归属和验收记录。

验收：玩法与数值没有未经允许的偏离；映射检查无遗漏；全量测试通过；新存档、阶段存档与结局路径有明确验证记录；发布构建可运行，相关源码与文档已提交推送。尚未验证的路径必须明确记录，不以抽样对照代替完整声明。

## 续接方式

当前五步均待执行。下次从第 1 步开始，每次优先完成一步。每步结束在此追加：完成范围、改动文件、验证命令与结果、未解决事项、提交号和下一步入口。测试使用阶段存档或加速夹具时，明确区分正常解锁验证与强制状态测试。
