# 亡灵之塔：开发进度与五步计划

盘点日期：2026-09-17。下方盘点与初次验证保留中断现场记录；最新状态以末尾各步执行记录为准。

## 固定要求

- 玩法、公式、价格、倍率、解锁、重置保留规则、随机机制和离线模拟均以 `AntimatterDimensionsSourceCode` 提交 `5409e320cecef96a917cca1dfb68f1f183e499ca` 为准，不自行设计或调整平衡。
- 名称与对象对应遵循《亡灵之塔-逐项内容映射表.md》和《反物质维度-映射参数索引.json》，保留源 ID 和对应关系。内部标识保留原名时，必须仍能精确追溯映射。
- 采用已确认的暗黑像素风。最新要求为左侧经营、右侧军团展示；覆盖此前相反的描述，当前第 2 步未验收实现须纠正。
- 军团场景只展示真实拥有的单位，不引入战斗、伤害、掉落、死亡消耗或额外收入。
- 每一步独立验证、记录结果并提交推送，避免一次性完成全部工作。已有无关改动不混入提交。

## 当前实际进度

| 部分 | 已存在的实现 | 尚不能认定完成的部分 |
|---|---|---|
| 源码与数值基础 | `vendor/antimatter` 保留原作；构建脚本在临时目录应用展示和平台适配；哈希清单覆盖 820 个文件 | 从当前源码重新完整构建并复核适配边界、依赖及可复现性 |
| 映射 | 767 条记录绑定源配置，具备词汇表、短语适配和典籍查询 | 绑定成功不等于所有动态说明、奖励、弹窗和后期页面已严格完成中文映射 |
| 界面 | React 外壳、资源栏、墓园背景、八族精灵、军团名册；Vue 内核通过 iframe 接入 | 左右位置已修正为左经营右军团；后期主要沿用原作组件，视觉和长文本仍需逐页验收 |
| 初期玩法 | 自定义军团页调用原购买、节律、筑塔、墓域及献祭逻辑 | 补齐边界交互与解锁提示验收 |
| 中后期 | 复用原作转生、轮回、创世、冥器、主宰及终局系统和页面 | 不等于已逐系统完成可操作性、文案、重置边界与全流程验收 |
| 存档与离线 | 独立本地存档键，沿用原序列化与离线模拟，已有保存和重载测试 | 导入导出、备份恢复、多槽、异常输入和升级兼容仍需专项验证 |
| 测试 | 已有源码校验、原版与主题版场景对照、浏览器测试和截图 | 测试仅抽样覆盖；强制解锁页面渲染不证明正常解锁流程完整 |
| 文档与版本 | 设计、映射及参考图已在历史提交中 | 中断前的大量实现仍未提交；旧 README 的“只有静态页面”描述已过时 |

实际架构为 React 外壳 + 原作 Vue 运行时，并非原设计提案中的全量 React 展示重写。后续以保留原数学规则和完成可用界面为目标，避免无必要的内核重写。

## 本次验证记录及限制

- `node scripts/verify-source.mjs`：通过，820 个上游文件未改变，767 个映射键唯一，现有构建目录中受检查的模拟源码与基线一致。
- `node node_modules/typescript/bin/tsc -b --pretty false`：通过。
- `pnpm lint`：退出码 0。
- `node tests/runtime.mjs`：报告 31 项通过、0 项失败，浏览器错误列表为空；包含 22 个数值对照场景、41 个页面渲染以及 320/375/414/768/1440 像素宽度检查。
- 浏览器测试使用现有 `public/engine` 与 `public/reference` 构建产物。本次没有重新运行完整构建；部分运行时源文件晚于现有产物，因此这些结果不能替代第 1 步的重新构建验收。
- 尚未完成自然推进至结局的全流程验收，不能据此宣称完整游戏开发完成。不估算缺乏依据的完成百分比。

## 原五步计划（用户追加第 6 步发布）

### 第 1 步：恢复可复现基线，收口中断现场

检查并归属所有未提交文件；核对原作提交、源码清单、适配白名单、依赖锁文件、构建脚本与存档隔离。重新构建原版对照与亡灵版，解决构建或数值对照失败，整理当前架构说明和实际启动命令。清理旧占位状态前先确认无引用。

验收：从源码完整构建成功，源码校验、类型检查、lint 和现有 31 项回归通过；确认测试针对本轮新产物。提交仅包含已核对的基线实现，并记录提交号。

### 第 2 步：完成确认风格与初期可玩闭环

调整为左经营、右军团；按参考图统一面板、按钮、字体、像素精灵与数值排版。展示真实单位和数量，动画暂停仅影响画面。核对八阶购买、每十购、最大购买、魂火节律、筑塔、墓域、献祭，以及首次转生的入口、条件与确认提示。

验收：真实点击完成购买和满足原条件的初期重置；军团显示与存档同步；桌面和移动端无关键按钮遮挡、溢出；视觉截图逐项核对参考风格。

### 第 3 步：完成转生至轮回、永夜阶段

逐页完成魂契、打破魂界、通灵议会、疫种、亡灵/破界/轮回试炼、执役自动购买、墓园祭坛、冥典树、轮回里程碑及永夜的映射和界面。验证研究前置、互斥、重洗、挑战特殊规则与重置保留项。

验收：每个系统均有与原版同状态的关键操作对照；覆盖购买成功与失败、解锁边界及重置；不能只靠开发测试强制打开页面。记录该阶段映射和页面检查清单。

### 第 4 步：完成创世、七位主宰与终局

逐页完成创世律令、亡者遗赠、冥器生成/装备/祭业/筛选、死寂双钟、塔灵敕令、七位主宰领域、追忆、炼金、虚冥、灾厄化身、死星、终焉封界、裂隙和万墓织机。保留原随机种子行为及敕令语法，不以词汇替换破坏脚本。

验收：各领域进入/退出和重置边界与原版一致；冥器随机与装备效果可对照；覆盖终焉规则变化、万墓织机阶段和结局触发。关键操作必须有可达入口，页面可渲染不能作为唯一验收依据。

### 第 5 步：全流程审计与交付

逐项审计 767 条映射及数据库之外的资源、阶位、动态说明、帮助、通知、确认框和结局文字。补齐导入导出、备份、多存档槽、离线、异常恢复测试；检查后期大数排版与性能。运行全量回归及跨阶段操作链，构建正式产物，补全文档、许可归属和验收记录。

验收：玩法与数值没有未经允许的偏离；映射检查无遗漏；全量测试通过；新存档、阶段存档与结局路径有明确验证记录；发布构建可运行，相关源码与文档已提交推送。尚未验证的路径必须明确记录，不以抽样对照代替完整声明。

## 续接方式

第 1 步已完成，第 2 步已有待验收实现，第 2–6 步交由 deadline-carl 在 4 小时预算内推进。具体接手条件见《亡灵之塔-deadline-carl任务书.md》。每步结束在此追加：完成范围、改动文件、验证命令与结果、未解决事项、提交号和下一步入口。测试使用阶段存档或加速夹具时，明确区分正常解锁验证与强制状态测试。

## 第 1 步执行记录（2026-09-17）

状态：完成。实现提交：`1041ad5`（`feat: 接入亡灵之塔原作运行基线与构建验证`）。

完成范围：

- 将中断前的原作快照、主题运行时、React 外壳、素材、桥接与测试收口为可运行基线；检查未提交文件归属，没有混入其他项目、依赖目录或生成产物。
- 逐文件对照相邻原作仓库，820 个文件的 SHA-256 均匹配固定提交；保留原作 MIT 许可，并将其复制到正式交付目录。
- 补充运行时输入指纹：开发启动识别过期产物并重建，浏览器测试拒绝缺失或过期的主题版/原版构建。验证过期构建被阻止、最新构建可直接复用。
- 原版对照构建可独立安装其锁定依赖；校验原版对照的模拟源码，并精确检查存档适配仅修改主存档与备份键。
- 移除无引用的 `TowerOverview` 和 `game/config.ts`、`game/state.ts`、`game/types.ts` 占位文件；原作状态成为唯一模拟来源。
- 重写 README 的架构、依赖、启动、重新构建、测试、存档和许可说明；增加正式产物冒烟检查。

验证结果：

| 命令或检查 | 结果 |
|---|---|
| `pnpm install --frozen-lockfile` | 通过，外壳锁文件无需改变 |
| 相邻原作快照逐文件核对 | 820 个文件全部一致 |
| `pnpm test` | 从本轮源码重新构建两套运行时；31 项通过、0 项失败、浏览器错误为空 |
| `node scripts/verify-source.mjs` | 上游、主题构建与原版对照的受检查模拟文件一致，767 个映射键唯一 |
| `pnpm typecheck` / `pnpm lint` | 通过 |
| `pnpm build` | 完整构建通过；补充许可复制后另执行 `pnpm exec vite build`，正式目录重新生成成功 |
| `pnpm test:production` | 正式预览载入、实际召唤及军团同步通过；许可匹配，未携带 `reference/` 测试目录 |
| `git diff --cached --check -- . ':!vendor'` | 项目改动无空白错误；上游快照保持原字节 |

改动入口：`scripts/`、`runtime/`、`src/App.*`、`src/game/bridge.ts`、`tests/`、`vite.config.ts`、`vendor/`、`public/art/` 和 README。测试输出保留于本地 `test-results/runtime-report.json`、`step1-full-test.log`、`step1-build.log` 及截图目录。

已知限制：保留上游依赖带来的 Browserslist 数据过期和包体大小构建警告，没有升级依赖或改动数学规则。依赖安装验证使用现有本地缓存，未声称完成全新离线机器安装验证。31 项为固定场景回归，41 页使用阶段夹具检查；尚不等同自然推进到结局。左右布局、后期文案和逐系统操作验收留在原定后续步骤。

下一步入口：执行第 2 步，调整左经营、右军团布局，按参考图核对像素界面，并完成八阶购买至首次转生的真实交互验收。继续复用上述基线和数值对照，不重写模拟内核。

## 第 6 步：GitHub Pages 在线试玩（2026-09-17 追加）

完成后实际发布 GitHub Pages，正确支持项目子路径、内核 iframe 及全部静态资源。验收公开 HTTPS 链接可访问、实际购买、军团同步与保存刷新恢复，不以只配置发布流程作为完成。最新左右布局统一为左经营、右军团。


## 直接开发七步计划（2026-09-18）

用户已停止 Deadline-Carl，后续直接开发；以下编号替代上文旧计划的续接编号。保留旧记录供追溯，不能据旧阶段编号推断当前验收完成。

1. **补齐中期系统**：破界升级、中期执役、疫种、研究与试炼例外、永夜升级和离线里程碑。实施与验证见 [直接开发第 1 步](./亡灵之塔-直接开发第一步.md)。
2. **补齐创世系统**：创世升级、冥器及筛选、创世后执役、塔灵敕令和相关操作链。
3. **补齐七位主宰**：自然解锁条件、领域机制、奖励、三合研究和特殊试炼规则。
4. **补齐终焉流程**：封界、裂隙、末日阶段、结局和重开。
5. **完成全局映射与界面审计**：767 条映射、数据库外动态文案、帮助、通知和全部后期页面；保持左经营右军团。
6. **完成综合验收**：历史与损坏存档、备份、多槽、长离线、跨阶段操作链、性能和全量验证。
7. **发布 GitHub Pages**：实际部署并验证公开链接、资源子路径、购买及保存刷新恢复。

本次接手的上轮遗留实现单独保存为检查点 `c60eedb`，不代表整体已验收。本轮 GitHub 登录有效，Pages 仍未发布。

直接开发第 1 步已完成：新增 14 组检查，修复中期文案与响应式面板，AC1–AC5 通过。完整结果与夹具边界见第一步记录；下一步为创世系统。本结论不表示后续六步完成。


直接开发第 2 步已完成：补齐创世律令事件、冥器与筛选、创世后执役、双钟和塔灵敕令操作检查，修复对应文案及窄屏布局。范围、阶段夹具和证据见 [直接开发第 2 步](./亡灵之塔-直接开发第二步.md)。下一步为第 3 步七位主宰；仍有第 3–7 步共五步待完成。

直接开发第 3 步已完成：补齐七位主宰进入、操作、领域完成或退出记录，覆盖 Enslaved、V、Ra、Laitela 的核心后期系统，并修复 Ra 追忆逻辑键被显示翻译污染、Laitela 低对比和 Pelle 窄屏溢出。范围、阶段夹具和证据见 [直接开发第 3 步](./亡灵之塔-直接开发第三步.md)。下一步为第 4 步终局闭环；仍有第 4–7 步共四步待完成。
