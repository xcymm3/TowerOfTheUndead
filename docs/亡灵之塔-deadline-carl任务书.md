# 亡灵之塔：第 2–6 步限时交付任务

用户于 2026-09-17 要求停止当前人工开发轮次，启动 deadline-carl，预算 4 小时。完成原计划第 2–5 步，并新增第 6 步：发布 GitHub Pages，支持在线试玩。最新布局要求为 **左经营、右军团**，覆盖旧文档、旧测试及未验收实现中的相反描述。

## 必须遵守的基线

- 原计划见《亡灵之塔-开发进度与五步计划.md》；第 1 步已完成，第 2 步目前仅有未验收实现，不能直接视为完成。
- 玩法、数值、价格、倍率、解锁、随机种子、重置保留与离线规则完全采用 AntimatterDimensionsSourceCode 固定提交 `5409e320cecef96a917cca1dfb68f1f183e499ca`，不得自行发挥。
- 严格执行《亡灵之塔-逐项内容映射表.md》和《反物质维度-映射参数索引.json》的 767 条映射。保留内部源 ID、原脚本语法及可追溯关系。
- 沿用已确认暗黑像素参考风格，桌面左经营右军团。军团展示真实拥有单位，允许只有展示，不新增战斗、伤害、掉落或额外收入。
- 复用现有 React 外壳、Vue 原作内核、820 文件哈希校验及原版状态对照；不为了验收重写模拟公式。强制状态夹具必须与正常解锁和真实操作证据区分。

## 强制验收范围

### 第 2 步：确认风格与初期可玩闭环

纠正左右布局，并同步 DOM、响应式样式、测试及文档。核对八阶购买、每十购、最大购买、魂火节律、筑塔、墓域、献祭、首次转生的入口、条件、确认和重置效果。军团数量与存档同步，画面暂停不停止模拟。真实点击并对照原版边界状态；桌面和 320/375/414/768/1024/1440 宽度无关键按钮遮挡或溢出，截图核对参考风格。

### 第 3 步：转生至轮回、永夜

完成魂契、打破魂界、通灵议会、疫种、亡灵/破界/轮回试炼、执役自动购买、墓园祭坛、冥典树、轮回里程碑和永夜的映射、界面与真实可操作入口。各系统购买成功/失败、解锁边界、挑战规则、研究前置/互斥/重洗、重置保留项须与原版同状态对照，列出逐系统证据，不能只以页面渲染代替验收。

### 第 4 步：创世、七位主宰与终局

完成创世律令、亡者遗赠、冥器生成/装备/祭业/筛选、死寂双钟、塔灵敕令、七位主宰领域、追忆、炼金、虚冥、灾厄化身、死星、终焉封界、裂隙和万墓织机。验证进入/退出与重置边界、随机与装备效果、终焉规则变化、织机阶段及结局触发；保留原随机种子行为和敕令语法，不以单纯渲染证明可玩。

### 第 5 步：全流程审计与交付质量

逐项审计全部 767 条映射及数据库之外的资源、阶位、动态说明、帮助、通知、确认框和结局文字。验证存档导入导出、备份恢复、多槽、离线、异常恢复与阶段兼容，大数排版及性能。完成源码校验、类型检查、lint、原版数值对照、跨阶段真实操作链和正式产物测试。记录新存档、阶段存档与结局路径的验证方法及限制；补全文档和许可。未覆盖路径诚实标为未验收，禁止用抽样测试宣称全部完成。

### 第 6 步：GitHub Pages 在线试玩

构建并实际发布至本仓库 GitHub Pages，提供可访问的 HTTPS 试玩链接。正确处理项目子路径 `/TowerOfTheUndead/`、iframe 内核、图片、字体和其他静态资源；不能仅配置 workflow 就宣称发布成功。在线验证页面成功载入、资源无 404、真实购买、军团同步、保存与刷新恢复，并保留在线验证证据。正式产物不得包含原版 reference 测试站、测试存档或本机秘密。

## Git 与部署授权

用户明确要求完成任务后自动提交推送，并授权发布 GitHub Pages。源码交付目标固定 `origin` 的 `main`，仓库 `https://github.com/xcymm3/TowerOfTheUndead`。最终源码提交主题冻结为 `feat: 完成亡灵之塔后续玩法验收与在线试玩`。不创建 tag 或 GitHub Release，不强推、不重写历史、不混入无关改动。

冻结阶段必须明确部署与验收顺序：允许为在线验收构建、启用 Pages、上传 Pages artifact 或向专用 `gh-pages` 部署分支提交推送已构建站点；这些仅限用户授权的站点发布。最终经验证的源码提交及向 `origin/main` 推送仍按监督器 delivery 合同执行。部署分支提交也遵循英文小写 type 加简体中文摘要。不得将尚未发布的站点标为已在线；如平台权限或认证阻塞，记录真实错误和未达成验收项。

## 接手现场及效率提示

- 原第 1 步实现提交 `1041ad5`，续接记录 `2f5a265`。本任务初始化前将当前第 2 步相关改动保存为待验收检查点，不代表第 2 步通过。
- 第 2 步增加 BoostRow/GalaxyRow/CrunchButton 原组件展示适配、通知与弹窗翻译、初期真实交互测试。现有左右布局及测试预期仍相反，务必修正。
- `tests/early-interactions.mjs` 集成到 runtime 测试，新增 7 组用例。近期修正了首次转生共用消息弹窗的时序问题，以及原版对照页面与主题页共享浏览器上下文引起的存档污染；需要独立复核最新结果。
- 初期转生测试用 120000ms 夹具避开同时触发快速转生成就提示，然后单独触发原作成就 55 的回调检查提示。这是测试夹具，不是玩法改动。
- 最新本地输出在 `test-results/step2-acceptance.log`、`runtime-report.json`、`step2-release-build.log`；这些是交接线索，不直接充当新验收证据。第二步验收文档仍标待验收。
- 构建机内存紧张，单次内核编译可能耗时 4–8 分钟；主题构建已设置 `parallel: false`。复用指纹确认未过期的产物，避免无意义重复构建，也不得跳过修改后的必要构建。
- `pnpm test` 重建两套内核；`pnpm test:runtime` 验证已有且指纹有效的产物；`pnpm build` 构建正式产物；`pnpm test:production` 测试正式产物。不要终止无关用户进程。
- 优先检查外壳 CSS 中 `/art/...` 绝对路径和 Vite base；iframe 已使用 `import.meta.env.BASE_URL`，但仍需真实子路径及线上验证。
- 4 小时是硬预算，不自动延长；按 deadline-aware 组织工作并预留 fresh verifier、发布与交付时间。时间不足时如实记录剩余项，验收标准不缩水。
